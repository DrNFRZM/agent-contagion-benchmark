# Direct dependencies used to regenerate the committed quick-run tables.
# Python 3.12. Transitive packages and rendered PNG pixels can still vary by platform.
networkx==3.6.1
numpy==2.5.3
pandas==3.0.6
matplotlib==3.11.2
PyYAML==6.0.3
pytest==9.1.1
